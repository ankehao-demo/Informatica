[pytest]
# `pythonpath` keeps the tests runnable with a bare `pytest` from this directory,
# with no install step and no PYTHONPATH juggling on a Databricks cluster.
pythonpath = src tests
testpaths = tests
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
